spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/notifyhub
    username: notifyhub
    password: localdev
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  spring:
    jpa:
      open-in-view: false